---
bank_name: "Session 1: Literals, Identifiers, and Variables — Practice"
---

1. Which of the following is a string literal?
a) 42
b) true
*c) "hello"
d) let

2. Which keyword should you use when a variable's value will NOT change?
a) let
b) var
*c) const
d) static

3. Which of the following is a valid JavaScript identifier?
a) 1stPlace
b) my name
*c) firstName
d) let

4. What does `console.log()` do?
a) Creates a new variable
b) Deletes a variable from memory
*c) Prints a value to the browser console
d) Declares a constant

5. What is the value of `x` after this code runs?
```
let x;
```
a) 0
b) null
c) ""
*d) undefined

6. Which of the following correctly declares a variable that might change?
a) const score = 10;
*b) let score = 10;
c) var = score 10;
d) score let = 10;

7. Which naming convention does JavaScript use for variables with multiple words?
a) snake_case
b) PascalCase
*c) camelCase
d) UPPER_CASE

8. True or False: You can reassign a value to a `const` variable after it is declared.
a) True
*b) False

9. What is wrong with this identifier: `my variable`?
a) It starts with a lowercase letter
b) It uses a reserved word
*c) It contains a space
d) It is too long

10. Which of the following is a number literal?
a) "42"
b) true
*c) 42
d) [42]

11. What will this code print?
```
const city = "Auckland";
console.log(city);
```
a) city
b) "city"
*c) Auckland
d) Nothing — const cannot be logged

12. True or False: `myVar` and `myvar` refer to the same variable in JavaScript.
a) True
*b) False

13. Which of the following is NOT a valid identifier?
a) _count
b) $price
c) totalScore
*d) return

14. What keyword should you avoid using in modern JavaScript?
a) let
b) const
*c) var
d) function

15. What does "declaring a variable" mean?
a) Giving it a new value
b) Deleting it
*c) Creating it and giving it a name
d) Printing it to the console

16. In JavaScript, does whitespace (spaces and blank lines) change how a program runs?
a) Yes — extra spaces cause syntax errors
b) Yes — blank lines slow down the program
*c) No — whitespace is ignored by JavaScript
d) Only in certain browsers

17. What does the `//` symbol do in JavaScript?
a) Divides two numbers
*b) Starts a single-line comment that JavaScript ignores
c) Opens a block of code
d) Declares a variable

18. Which of the following starts a multi-line comment?
*a) `/*`
b) `//`
c) `<!--`
d) `##`

19. True or False: A variable named `userName` and a variable named `username` are the same in JavaScript.
True
*False

20. What does `document.getElementById("output")` do?
a) Creates a new HTML element with the id "output"
b) Deletes the element with the id "output"
*c) Finds and returns the existing HTML element that has the id "output"
d) Logs "output" to the console

21. What does the `.textContent` property do when you assign a value to it?
a) Adds a new HTML attribute to the element
b) Changes the CSS style of the element
*c) Replaces the visible text inside the element
d) Removes the element from the page

22. What does this code print?
```javascript
// let score = 100;
console.log("done");
```
a) 100
b) score
*c) done
d) Nothing — the comment causes an error

23. Look at this code. What does it log?
```javascript
let a = 5;
let A = 50;
console.log(a);
```
a) 50
b) undefined
*c) 5
d) An error — you cannot have both `a` and `A`

24. What does this code display on the page?
```javascript
const city = "Wellington";
document.getElementById("result").textContent = city;
```
a) "city"
b) "result"
*c) "Wellington"
d) Nothing — you must use innerHTML instead of textContent
